"""Entrypoint for running the Streamlit app.

Usage:
- Prefer: `streamlit run app.py`
- Or: `python app.py` (will call the app runner, but Streamlit's CLI is recommended)
"""

from mycore import main


if __name__ == "__main__":
    main()
